# Optional. Amboss Magma API key — generate at:
# https://account.amboss.tech/settings/api-keys
# Leave unset to use anonymous access (Magma will auto-create a temporary account).
MAGMA_API_KEY=

# Optional. Override the Magma GraphQL endpoint (defaults to production).
# MAGMA_GRAPHQL_ENDPOINT=https://magma.amboss.tech/graphql
